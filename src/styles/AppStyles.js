import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 10,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#000"
  },
  buttonText: {
    fontSize: 20,
    textAlign: "center",
    color: "#000"
  }
});
